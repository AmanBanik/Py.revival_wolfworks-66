# Set methods T1
s1={1,77,20,'aman','coder',3.1415}

# s1.add('boomer')# add single element
# s1.update({1,2})# add multiple elements via lsts tuple sets
# s1.remove('aman')# removes an element
# s1.discard('rathi')# no error but does the same
# print(s1.pop,s1)# removes and returns any arbitary element
# s1.clear()# returns the set emptied out
# print(len(s1))# returns the length of the set
print(s1)